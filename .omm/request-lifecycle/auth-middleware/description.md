Request authorization layer (server/src/auth/). Checks JWT bearer tokens or host-derived session cookies before allowing access to protected routes. JwtAuthorizer validates HS256 JWTs signed with a persisted secret. HeadlessTokenAuthorizer additionally accepts ?token= query params for headless browser auth. LoginManager handles credential verification and JWT issuance with brute-force lockout protection.
