SilverBullet has two distinct scripting systems that serve different purposes: PlugOS (compiled TypeScript, worker-isolated, defines hooks/syscalls) and Space Lua (interpreted in main thread, embedded in markdown, used for live templates and queries). They are separate systems with different APIs and lifecycles, though Space Lua can call into PlugOS syscalls via the space_lua_api bridge.
