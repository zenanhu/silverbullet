SLIQ (Space Lua Integrated Query) engine (client/space_lua/query_collection.ts). A custom Lua-based query language that evaluates query expressions against the ObjectIndex. Supports filters (where), sorting, pagination (limit, offset), projections (select), and aggregations. Powers live templates, dynamic widgets, and the sb query CLI command. Queries are Lua expressions wrapped in query[[...]] syntax.
