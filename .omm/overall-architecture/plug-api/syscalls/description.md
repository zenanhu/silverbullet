Domain-specific syscall wrappers (plug-api/syscalls/, 17 modules). Each module exports typed async functions that call syscall("domain.functionName", ...args). Covers: editor (~50 functions for text, cursor, navigation, panels, commands), space (~17 for file CRUD), system (~13 for plug lifecycle, config), datastore (~10 for KV store), index (~22 for object queries), markdown (parse/render), event (dispatch/listen), mq (message queue), sync, shell, fetch, lua (parse/eval), config, client_store, asset, language, yaml, jsonschema.
