Late-binding syscall dispatch (plug-api/syscall.ts). The single injection point between plug code and the SilverBullet runtime. Exports syscall(name, ...args) that delegates to globalThis.syscall(name, ...args). The runtime sets globalThis.syscall before executing any plug code. Provides a dummy fallback for test contexts. Every domain-specific syscall wrapper module imports this.
