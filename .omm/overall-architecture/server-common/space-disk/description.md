Disk-backed storage (server-common/src/space/disk.rs). The primary filesystem storage engine. Reads/writes space files from a real directory with path traversal guards. Uses walkdir with .gitignore filtering for file listing. Timestamps use created (btime) with fallback to modified (mtime). Write supports optional last_modified restoration for sync. MIME type lookup with custom overrides for .md → text/markdown.
