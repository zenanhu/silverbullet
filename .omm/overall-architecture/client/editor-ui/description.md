Main UI shell (client/editor_ui.tsx, ~680 lines + reducer.ts). A Preact component that renders the top navigation bar, command palette (Ctrl+K), anything picker, filter list, modal dialogs, and the CodeMirror editor container. Uses a reducer pattern for view state management (AppViewState, Action union type). State includes current page, UI panel visibility, command list, and page list.
