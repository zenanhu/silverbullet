Schema-only plug (plugs/core/core.plug.yaml, 1 file). No executable functions. Defines the JSON Schema for built-in SilverBullet object types: syscall, command, space-config, space-style, space-script. Also defines the configuration schema keys: indexPage, maximumDocumentSize, objectDecorators, spaceIgnore. This is the single versioned source of truth for all built-in object and configuration shapes consumed by the index plug and configuration-manager.
