The storage architecture uses decorator/composite patterns extensively: ReadOnly wraps any writable backend, Encrypted wraps any KvPrimitives, Fallthrough composites two backends with primary-over-fallback semantics, Evented wraps any backend with event emission, Filtered wraps with path prefix filtering. All backends implement the same trait/interface, enabling swap at any layer.
