The FallthroughSpacePrimitives is key to the deployment model: it layers the read-only base_fs (built-in libraries, templates, defaults) on top of the user's writable space. This allows SilverBullet to ship with default content while letting users override any file. Writes to paths that only exist in the fallthrough layer are rejected — the user must have an existing file to overwrite.
